<!DOCTYPE html>
<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="forgotstyle.css">
    <link rel="stylesheet" href="https://unpkg.com/boxicons@latest/css/boxicons.min.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            scroll-behavior: smooth;
            list-style: none;
            text-decoration: none;
        }

        *::selection {
            background: var(--main-color);
            color: #fff;
        }

        section {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: rgb(0, 0, 0);
            background-image: url(assert/forget.jpg);
            background-position: center;
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        .container {
            position: relative;
            width: 380px;
            height: 520px;
            background: transparent;
            border: 2px solid rgba(255, 255, 255, 0.5);
            backdrop-filter: blur(5px);
            border-radius: 8px;
            overflow: hidden;
        }

        .upper {
            position: relative;
            width: 380px;
            height: 200px;
            background: transparent;
            border: none;
            outline: none;
            backdrop-filter: blur(5px);
            border-radius: 8px;
        }

        .lower {
            position: relative;
            width: 380px;
            height: 270px;
            background: transparent;
            border: none;
            outline: none;
            backdrop-filter: blur(5px);
            border-radius: 8px;
        }

        .lower form {
            position: absolute;
            inset: 4px;
            background: transparent;
            backdrop-filter: blur(5px);
            padding: 50px 40px;
            border-radius: 8px;
            z-index: 2;
            display: flex;
            flex-direction: column;
        }

        .upper form {
            position: absolute;
            inset: 4px;
            background: transparent;
            backdrop-filter: blur(5px);
            padding: 50px 40px;
            border-radius: 8px;
            z-index: 2;
            display: flex;
            flex-direction: column;
        }

        .upper h2 {
            color: #0000009a;
            font-weight: 500;
            text-align: center;
            letter-spacing: 0.1em;
        }

        .lower h2 {
            color: #ffffffd7;
            font-weight: 500;
            text-align: center;
            letter-spacing: 0.1em;
        }

        .lower .input-box {
            position: relative;
            width: 300px;
            margin-top: 10px;
        }

        .upper .input-box {
            position: relative;
            width: 300px;
            margin-top: 10px;
        }

        .input-box input {
            position: relative;
            width: 100%;
            padding: 5px 5px 5px;
            background: transparent;
            outline: none;
            border: none;
            box-shadow: none;
            color: #23242ac3;
            font-size: 1em;
            letter-spacing: 0.05em;
            transition: 0.5s;
            z-index: 10;
        }

        .input-box span {
            position: absolute;
            left: 0;
            pointer-events: none;
            color: #fff;
            font-size: 1em;
            letter-spacing: 0.05em;
            transition: 0.5s;
        }

        .input-box input:valid~span,
        .input-box input:focus~span {
            color: #fff;
            font-size: 0.75em;
            transform: translateY(-10px);
        }

        .input-box i {
            position: absolute;
            left: 0;
            bottom: 0;
            width: 100%;
            height: 2px;
            background: #ffffff71;
            border-radius: 4px;
            overflow: hidden;
        }

        button {
            margin: 25px 0;
            border: none;
            outline: none;
            padding: 9px 25px;
            background: #ffffff8b;
            cursor: pointer;
            font-size: 0.9rem;
            border-radius: 4px;
            font-weight: 600;
            width: 200px;
            margin-top: 10px;
        }

        button:active {
            opacity: 0.8;
        }

        .input-box input:valid~i,
        .input-box input:focus~i {
            height: 25px;
        }

        .container {
            animation: pulse 1.5s ease-in-out;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }

            100% {
                transform: scale(1);
            }
        }
    </style>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <title>Forgot Password</title>
</head>

<body>
    <section>
        <div class="container">
            <div class="upper">
                <form class="farm" action="/forget" method="POST">
                    <h2>Forgot Password</h2>
                    <div class="input-box">
                        <ion-icon name="mail-outline"></ion-icon>
                        <input type="email" id="email" name="email" required>
                        <i></i>
                        <span><label for="">email</label></span>
                    </div>
                    <button type="submit">send</button>
                </form>
            </div>
            <% if(s){ %>
                <div class="lower">
                    <form class="farm" action="/verify" method="POST" onsubmit="return validateForm()">
                        <h2>Otp sent sucessfully</h2>
                        <div class="input-box">
                            <ion-icon name="mail-outline"></ion-icon>
                            <input type="email" id="email" name="email" required>
                            <span><label for="email">email</label></span>
                            <i></i>
                        </div>
                        <div class="input-box">
                            <ion-icon name="checkmark-done-circle-outline"></ion-icon>
                            <input type="otp" id="otp" name="otp" required>
                            <span><label for="otp">otp</label></span>
                            <i></i>
                        </div>
                        <div class="input-box">
                            <ion-icon name="lock-closed-outline"></ion-icon>
                            <input type="password" id="password" name="password" required>
                            <span><label for="password">new Password</label></span>
                            <i></i>
                        </div>
                        <button type="submit">Reset Password</button>
                    </form>
                </div>
                <% } %>
        </div>
    </section>
    <script>
        function validateForm() {
            var passwordInput = document.getElementById("password");
            var passwordPattern = /[!@#$%^&*(),.?":{}|<>]/;
            if (!passwordPattern.test(passwordInput.value)) {
                alert("Password must contain at least one special character"); // Show alert box
                return false; // Prevent form submission
            }
            // Form is valid, allow form submission
            return true;
        }
    </script>
    <script src="your-fontawesome-kit.js"></script>
    <script src="login.js"></script>
    <script src="index.js"></script>
    <script type="module" src="https://unpkg.com/@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
</body>

</html>